#pragma once
#include "common.cpp"
using namespace dalt;
Vec<Str> words{"out", "output", "puton", "in", "input", "one"};
void SolveOne(int test_id, IStream &in, OStream &out) {
  Str s;
  in >> s;
  Vec<bool> ok(Size(s) + 1);
  ok[0] = true;
  for(int i = 0; i < Size(s); i++) {
    if(!ok[i]) {
      continue;
    }
    for(var &word : words) {
      if(s.substr(i, Size(word)) == word) {
        ok[i + Size(word)] = true;
      }
    }
  }
  out << (ok[Size(s)] ? "YES" : "NO") << '\n';
}

void SolveMulti(IStream &in, OStream &out) {
  //std::ifstream input("in");
  int num_of_input = 1;
  in >> num_of_input;
  for (int i = 0; i < num_of_input; i++) {
    //SolveOne(i + 1, input, out);
	SolveOne(i + 1, in, out);
  }
}